<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>VR01_PreventCurrencyEdit</fullName>
    <active>true</active>
    <description>Prevents currency modification on a Shareclass after creation, unless the user is an admin or has BypassValidationRules__c set to true.</description>
    <errorConditionFormula>AND(
  ISCHANGED(CurrencyIsoCode),
  NOT($User.BypassValidationRules__c),
  $Profile.Name &lt;&gt; &quot;Carmignac - System Administrator&quot;
)</errorConditionFormula>
    <errorDisplayField>Currency__c</errorDisplayField>
    <errorMessage>The currency cannot be changed after the record is created.</errorMessage>
</ValidationRule>
