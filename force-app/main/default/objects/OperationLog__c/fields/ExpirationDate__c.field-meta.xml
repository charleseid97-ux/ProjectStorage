<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ExpirationDate__c</fullName>
    <defaultValue>TODAY()+100</defaultValue>
    <description>Date used by the purge batch. Defaults to today plus 100 days, but developers can override it per log entry.</description>
    <label>Expiration Date</label>
    <required>false</required>
    <type>Date</type>
</CustomField>
