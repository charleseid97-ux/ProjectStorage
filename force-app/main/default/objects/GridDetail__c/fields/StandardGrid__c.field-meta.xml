<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>StandardGrid__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Populated only when Grid__c is a custom grid. The referenced standard grid is used to determine the grid rate and the associated share rates.</description>
    <externalId>false</externalId>
    <inlineHelpText>Populated only when Grid__c is a custom grid. The referenced standard grid is used to determine the grid rate and the associated share rates.</inlineHelpText>
    <label>Standard Grid</label>
    <referenceTo>Grid__c</referenceTo>
    <relationshipLabel>Custom Grid Details</relationshipLabel>
    <relationshipName>CustomGridDetails</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
